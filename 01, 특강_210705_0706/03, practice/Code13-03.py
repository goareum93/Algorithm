## 클래스와 함수 선언 부분
def binSearch(ary, fData):
    pos = -1
    start = -
    end = len(ary) -1

    while (start <= end):
        mid = (start + end) //2
        if fData == ary[mid]
            return mid
        elif fData > ary[mid]
            start = mid + 1
        else:
            end = mid -1

        return pos